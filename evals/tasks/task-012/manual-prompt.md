# Manual Prompt — task-012

Find and replace all occurrences of 'Sign Up' with 'Get Started Free' in the navigation components. Check src/components/Navbar.tsx and src/components/MobileNav.tsx. Also update any auth buttons that use the old label.
