# Manual Prompt — task-004

Modify the DeleteButton component in src/components/DeleteButton.tsx. Reduce size by using px-2 py-1 text-xs. Change background to bg-red-500 hover:bg-red-600. Add a TrashIcon from lucide-react before the 'Delete' text with w-3 h-3.
