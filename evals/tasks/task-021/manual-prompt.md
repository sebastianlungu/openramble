# Manual Prompt — task-021

Implement a mobile hamburger menu in src/components/Navbar.tsx. Add a hamburger icon button visible only on screens below md breakpoint. On click, show a slide-in menu from the left with nav links. Use a state variable isOpen with a transition overlay. Add close-on-outside-click and close-on-link-click behavior.
