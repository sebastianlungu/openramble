# Manual Prompt — task-026

Add required field indicators to the checkout form in src/app/checkout/page.tsx. For each required field label, append a red asterisk (<span className='text-red-500'>*</span>). Required fields: name, email, address, city, zip, card number. Add a note at the top: '* Required fields'. Ensure screen readers get the required attribute.
