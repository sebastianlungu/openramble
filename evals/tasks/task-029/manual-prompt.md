# Manual Prompt — task-029

Add aria-labels to all icon-only buttons across the app. Search for buttons containing only icon components with no text children. Add descriptive aria-label attributes: CloseButton -> 'Close dialog', MenuButton -> 'Open navigation menu', ShareButton -> 'Share this page', SearchButton -> 'Search'. Target files: CloseButton.tsx, MenuButton.tsx, ShareButton.tsx, SearchButton.tsx.
