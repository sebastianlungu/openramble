# OmniCapture Output — task-029

[OmniCaptain generated prompt — placeholder] Add aria-labels to icon-only buttons for screen reader support. Target files: all button components with icon-only rendering. Provide descriptive labels for each context.
