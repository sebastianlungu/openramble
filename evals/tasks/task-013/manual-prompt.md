# Manual Prompt — task-013

Update the email validation error message in the form validation logic. Check src/lib/validators.ts or src/components/forms/. Change the error string for invalid email to 'Please enter a valid email address, e.g. name@domain.com'.
