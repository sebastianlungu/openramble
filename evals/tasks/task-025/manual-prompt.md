# Manual Prompt — task-025

Add character count to the bio textarea in ProfileForm.tsx. Display remaining characters below the textarea (e.g., '342/500'). Use the maxLength attribute to limit input to 500 characters. Style the counter in text-sm text-gray-500. When below 50 characters, change to text-amber-500 as a warning.
