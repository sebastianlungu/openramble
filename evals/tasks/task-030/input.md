# task-030: Accessibility

Our form inputs don't have proper labels — the labels are just spans, not actual label elements. Fix all form inputs so they use proper <label> elements with htmlFor attributes matching the input ids, for screen reader compatibility.
