# Manual Prompt — task-030

Audit and fix form input labels across the application. In all form components (LoginForm, SignupForm, CheckoutForm, ContactForm), replace span-based labels with proper <label htmlFor='field-id'> elements. Ensure each input has a matching id. Add visually-hidden labels for placeholder-only inputs. Test with a screen reader.
