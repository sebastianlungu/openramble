# Manual Prompt — task-023

Fix hero image cropping on mobile in src/components/Hero.tsx. Change the image container from a fixed height to responsive. Use object-contain instead of object-cover. On mobile, give the image a max-h-64. Add aspect-ratio support. Test with the existing image at different breakpoints.
