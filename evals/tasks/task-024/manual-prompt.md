# Manual Prompt — task-024

Add inline email validation to the signup form in src/app/signup/page.tsx. Use a regex pattern to validate email format on every keystroke. Show a green CheckCircle icon when valid, red XCircle when invalid, using lucide-react icons. Only validate after the user has typed at least 3 characters. Add appropriate aria attributes for accessibility.
