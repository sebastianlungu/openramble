# Manual Prompt — task-028

Update active nav link styling in src/components/Navbar.tsx. Use Next.js usePathname to detect active route. Apply font-bold and a colored bottom border (border-b-2 border-blue-500) to the active link. Use transition-all for smooth border animation. Ensure the indicator works for nested routes using startsWith matching.
