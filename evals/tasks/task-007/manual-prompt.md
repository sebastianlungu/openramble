# Manual Prompt — task-007

Reduce the top margin in the main content wrapper. In src/components/Layout.tsx, find the <main> element and change mt-16 to mt-8. Verify this doesn't cause overlap with the fixed header.
