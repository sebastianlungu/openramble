# Manual Prompt — task-020

Create a BackToTop button component in src/components/BackToTop.tsx. Use a scroll event listener to track scroll position. Show the button when scrollY > 300. Position fixed, bottom-6 right-6. Use an ArrowUp icon. On click, call window.scrollTo({ top: 0, behavior: 'smooth' }). Add to src/components/Layout.tsx.
