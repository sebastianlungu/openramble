# Manual Prompt — task-017

Remove the Banner component and all its usages. Delete src/components/Banner.tsx. Remove the <Banner /> import and usage from src/components/Layout.tsx. Check for any tests referencing the Banner component and remove those too.
