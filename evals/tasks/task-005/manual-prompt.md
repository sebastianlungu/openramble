# Manual Prompt — task-005

Update the global secondary button styles in the Button component at src/components/ui/Button.tsx. Change the secondary variant hover state from hover:bg-gray-100 to hover:bg-blue-50 hover:text-blue-600. Ensure this applies to all instances using variant='secondary'.
