# Manual Prompt — task-019

Replace the testimonial carousel with a grid layout. In src/app/page.tsx, remove the Carousel import and usage. Create a new TestimonialGrid component in src/components/TestimonialGrid.tsx using a 3-column grid (grid-cols-1 md:grid-cols-3). Each testimonial card should show: quote, author name, role, and company. Use the existing testimonial data array.
