# Manual Prompt — task-008

Standardize the left padding on all sidebar navigation items in src/components/Sidebar.tsx. Set a consistent pl-4 on all NavLink items. Remove any custom padding classes that might override the base spacing.
