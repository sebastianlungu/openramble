# Manual Prompt — task-009

Add a max-width wrapper to the blog post layout in src/app/blog/[slug]/page.tsx. Wrap the content in a container with max-w-prose (720px) and mx-auto for centering. Ensure images can still go full-width with a negative margin technique.
